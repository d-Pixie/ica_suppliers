<%= fields_for "project[category_attributes][]", category do |category_form| %>
<%# TODO Fix so that selected categories get "use" class on reload after validation error (currently using JavaScript) %>
<div class="category unused<%= " use" if @categories_ids && @categories_ids.include?(category.id) %>" <%= 'id=cat_'+category.id.to_s %>>
    <span><%= category.name %></span>
    <div><%= image_tag(category.icon) %></div>
</div>
<% end %>